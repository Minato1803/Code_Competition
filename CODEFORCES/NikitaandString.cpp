#include <bits/stdc++.h>
using namespace std;
#define ll long long

int main()
{
	ios_base::sync_with_stdio(false);
	cin.tie(0);
	string s;
	cin>>s;
	ll count=0;
	vector <ll> anb;
	if(s[0]=='a')
	{	
		for(ll i=1;i<s.length();i++)
		{
			if(s[i]==b)
		}
	}
	cout<<count;
	return 0;
}
